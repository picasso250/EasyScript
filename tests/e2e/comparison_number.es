// This test file will be split into multiple tests to give more granular results.
// For now, we will test them all at once.

let a = 10;
let b = 20;

// Number comparisons
(a < b) == true;
// expect: true
